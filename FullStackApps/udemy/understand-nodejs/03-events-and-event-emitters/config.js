// magic strings saved to one central location, reduces likelihood of typos
// plus the IDE can auto complete there entry on use

module.exports = {
	events: {
		GREET: 'greet',
		CLICK: ['click']
	}
};