'use strict';
const mocha = require('mocha');
const {expect} = require('chai');

// test suite
describe('Mocha', ()=>{
	// unit tests - specs
	it('should run our tests with npm', ()=>{
		 expect(true).to.be.ok;
	});
});
