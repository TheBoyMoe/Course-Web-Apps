doctype html
html(lang="en")
  head
    title #{post.title}
  body
    section.post
      div.container.text-right
        a(href="").text-faded view all
        div.row
          div.col-lg-8.col-lg-offset-2.text-center
            h2.section-heading #{post.title}
            
            hr.light
            
            p.text-faded #{post.description}
            div.article #{post.description}